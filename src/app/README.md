# App shell

Tauri + React wiring: camera, canvas, properties panels (design doc §3,
§4.6, §8). The Tauri scaffold itself (src-tauri/) is created later via
`npm create tauri-app` once there's something worth rendering.

Not started.
