# App shell

Browser web app (Vite + React + TS) wiring: camera, canvas, properties
panels (design doc §3, §4.6, §8 — revised, see `_brain/FLUXBOARD.md`
FBD009). Runs in a tab, local single-player, no server. No Tauri for
now; avoid Tauri-only APIs so a later PWA or Tauri-wrap stays possible.

Not started.
