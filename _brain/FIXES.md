# FIXES.md
# FluxBoard verified solution registry. Project-local IDs (FBF00x). Empty at brain onboarding.
---
| ID | FIXES | SOLUTION | SESSION |
|----|-------|----------|---------|
| FBF001 | FBB001 | `FluxCanvas`'s render loop now keeps its own `animElapsedMs` clock, separate from the sim driver's clock and from raw wall time — it only accumulates frame-to-frame delta while `driver.isRunning()` is true, and that clock (not wall time) feeds both the conveyor belt-tick phase and the circling item rotation. RUN/HOLD now freezes every visual, not just item position. | SES014 |
| FBF002 | FBB002 | `InterpolatedSimDriver.getRenderItems()` now only blends prev/curr progress when `prev.edgeId === curr.edgeId`. If an item's edge changed between the two snapshots (forwarded mid-tick), it renders at `curr.progress` directly — same treatment as a freshly-spawned item (holds at its actual position for that frame window), instead of blending two different curves' progress values as if they were one. Added a regression test (`nodeRegistry`-style direct-numbers scenario) plus verified against the pre-fix code by compiling both versions outside vitest and comparing actual output. | SES015 |
| FBF003 | FBB003 | `SketchSegmentProperties` and `PathShapeField` both decoupled their Linear/Curve type state from the live bow value — each now tracks its own type state independently plus a raw-text buffer (`bowText`) that only commits a value to the model when it actually parses to a finite number; the type only flips to Linear on blur, once the value has genuinely settled at exactly 0, not on every keystroke while typing toward/through a negative number. Verified via a 17-assertion Node.js simulation covering every problematic sequence Falcon described. | SES038 |
| FBF004 | FBB004 | New `targetBufferIsFull` check in `buffer.ts`'s `tryDrain` — before dequeuing, it now peeks the candidate output edge's target (via `NodeTickContext.getNode`/`getNodeState`) and, if it's a buffer already at capacity, holds for that tick instead of forwarding blind, the same "hold" outcome as having no active output edge at all. Only buffer-kind targets are checked; every other target kind keeps its original eager-drain behavior. Falcon confirmed live via a 7-Silo docked chain, items now evenly distributed instead of piling at the end. | SES043 |
| FBF005 | FBB005 | `nodeSkin.ts`'s `drawNode` dim path replaced `ctx.globalAlpha *= 0.4` with `ctx.filter = 'brightness(0.55)'` — darkens the node's own source RGB while leaving alpha (and the grid behind it) untouched, a true opaque darken instead of a blend-toward-background transparency. Falcon confirmed: "ok its now fine." | SES044 |
| FBF006 | FBB006 | `sourcePort`/`targetPort` redefined to just BE the octagon anchor (0-7) a wire is physically drawn from/to, set at every edge-creation call site in `App.tsx` (`handleCreateEdge`, both sketch-conversion paths, docking, group-duplicate) instead of a hardcoded 0, plus a new `persistence.ts` `syncEdgePortsToAnchors` migration that reconciles any already-saved edge (and the built-in demo graph) on load. Distributor's weighted-mode field list, already keyed by `sourcePort`, needed no logic change — drawing wires from different octagon sides now gives each its own port automatically. Verified via `tsc --noEmit` + vitest (233 passing, 3 new tests, same 2 pre-existing unrelated failures); not yet exercised live by Falcon on his real project. | SES046 |
