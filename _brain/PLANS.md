# PLANS.md
# FluxBoard backlog — deferred builds and features not yet in active scope.
# Promotion to TEMPORARIES.md/FLUXBOARD.md STATE requires explicit Falcon instruction.
---
FBP001 | DEFERRED | Open property bag for items (beyond flat type tag, design doc §4.7) — deferred until the mixer's recipe matcher is actually being built (Milestone 3+)
FBP002 | DEFERRED | Manual z-order vs. automatic isometric depth-sort reconciliation (design doc §3, §4.5) — proposed answer: manual z-order as tie-breaker on top of auto depth-sort; not needed until the iso milestone (6)
FBP003 | IMPLEMENTED | Edge-gated "no viable output" fallback (design doc §7) — implemented in the sorter (SES011): a matched-but-gated-off output edge is treated the same as an unmatched item, subject to unmatchedPolicy ('hold' parks/retries, 'drop' destroys + tracks droppedCount)
FBP004 | DEFERRED | Buffer drainPolicy on-idle variant (design doc §4.2) — deferred on top of continuous-only v1 drain behavior
FBP005 | DEFERRED | Isometric mode (design doc §3, roadmap Milestone 6, stretch) — camera projection toggle, mode-specific skin assets, depth-sorted draw order. App ships without it if not reached.
FBP006 | DONE | USER_GUIDE.md written — see _brain/USER_GUIDE.md (SES002)
FBP007 | DEFERRED | FluxBoard Web — Canva-style hosted web build, separate future variant, reuses src/core + src/floor + src/skin unchanged, only swaps the platform adapter for browser storage instead of Tauri SQL plugin. Not started; FluxBoard PC (Tauri) is primary for now.
FBP008 | DEFERRED | UI chrome for managing the app — ribbon/tabs/panels/layers/properties surfaces (Falcon raised this explicitly, wants it discussed as its own topic). Overlaps design doc §4.6 (node properties panels) and roadmap Milestone 5 (port and interaction polish — config panels, z-order controls). Deliberately held until after Milestone 3 (full node registry) so there's real per-node config (recipes, rules, capacities) to design panels against, not just source/sink. Needs its own design conversation before building: panel layout (ribbon vs. sidebar vs. tabs), docking/floating behavior, what a "layers" concept means here beyond z-order.
FBP008 | RESOLVED | Discussed with Falcon after Milestone 4 (SES016): ribbon/tabs are premature (nothing yet to switch between — one graph, not many commands). Decision: minimal chrome only — one left-docked node palette (click a kind, click canvas to place) + one right-docked properties panel (edit the selection's logic config, edge ports/flowRate/gate, skin style, z-order) — folded directly into Milestone 5, not a separate ground-up shell project. Ribbon/tabs revisited only if a real need shows up later (multiple saved graphs, many more commands).
FBP009 | DEFERRED | Precise per-socket drag-wiring (pick a specific one of the 8 octagon port anchors, not just node-body-to-node-body) and node/edge deletion from the UI — both intentionally out of Milestone 5's first pass (SES016). Socket-precise wiring needs a real convention for how the 8 geometric anchors map to the sourcePort/targetPort integers node handlers already use, which doesn't exist yet; body-to-body drag + numeric port fields in the properties panel is the interim. Deletion is a straightforward GraphModel addition (removeNode/removeEdge + cleanup of runtime state/floor/skin) whenever Falcon wants it.
FBP009 | DONE | Full resolution (SES019): per-socket drag-wiring shipped on top of SES017's deletion half. FloorLayout now owns 8 octagon-anchor bookings per node (nearest-free-anchor auto-pick, max 8 paths/node, anchor stays fixed across a node move) — deliberately independent of GraphModel's sourcePort/targetPort integers, which remain plain routing indices untouched by this. That coupling (or lack of it) was the open question originally deferring this id; resolved as "keep them separate" per Falcon's framing (2026-09-03) — revisit only if he wants routing tied to physical sides later.
FBP010 | DEFERRED | Node lock also blocking deletion, not just drag-to-move (SES017 scoped lock to position only, per Falcon's own phrasing "move(if unlocked position)") — revisit if Falcon wants locked nodes protected from Delete too.
FBP011 | DEFERRED | OBJECTS tab — item-type registry (create/modify object types, basic shapes/sizes, round circle as the built-in default) — Falcon explicitly said "we can expound more of it later"; left as a non-functional "coming soon" placeholder in the wireframe UI-chrome build (SES018) until spec'd further.
FBP011 | RESOLVED (first-pass) | Built SES032, on Falcon's explicit go-ahead: `skin/ObjectRegistry.ts` (shape circle/square/triangle + size + color per item type, built-in 'item' default), `app/ObjectRegistryManager.tsx` modal (create/edit/delete, delete refuses the default and any still-referenced type), source/sorter/mixer item-type fields now dropdowns off the registry, `FluxCanvas`/`pathSkin.ts` render items per their registered shape/size/color, persisted per-project. Not yet built: a full custom polygon shape editor (explicitly deferred in favor of the 3 presets), and FBP012's object-type -> path-style taxonomy this registry could eventually feed.
FBP012 | DEFERRED | Object-type -> path-style taxonomy Falcon sketched (solid -> conveyor/pipe/transparent, fluid -> pipe, power -> wire, signal/other -> transparent) — flagged explicitly as "future only." 'pipe'/'wire' aren't real EdgeStyle values yet — only transparent/conveyor/glassTube exist in pathSkin.ts today.
FBP013 | DEFERRED | "LAYER 1-5" spatial z-axis/floor stacking (wireframe's second palette-strip row) with elevator/connector nodes linking layers — Falcon flagged as "an advance feature." Explicitly a DIFFERENT concept from the existing Logic/Floor/Skin architecture layers — naming collision noted to avoid future confusion.
FBP014 | DEFERRED | Tools tab (multiple-selection tool, move tool, etc.) and File tab (create new projects, manage/switch between multiple existing/saved projects) on the top-level UI, alongside/near the existing header — Falcon (2026-09-03): "remember that i will add tools tab... and the file tab... on the top. we can introduce more on later builds." Explicitly future/deferred, not started. File tab implies multiple named save files eventually — a real change from today's one-fixed-filename autosave (FBD010 rev.3, SES024); that stays as-is until this is picked up.
FBP014 | HALF-RESOLVED | File tab built (SES026, FBT018): create/switch/rename/delete multiple named projects, `persistence.ts`'s manifest + `projects/<id>.json`. Tools tab (multiple-selection tool, move tool) still deferred, not started — Falcon's own words on it were "we can introduce more on later builds," unlike File which he asked for outright once he noticed it missing.
FBP014 | RESOLVED | Tools tab's remaining piece built (SES033, FBT023): Multi-select (marquee + click-to-toggle), Duplicate (offset clone, keeps internal edges), Pan (forces every drag to pan) — Falcon (2026-09-05): "activate the modify functionalities especially the multiselect." Group drag-move (once a multi selection exists, dragging any member moves the whole group) came along with it as the natural extension of having a real multi-select. File tab (SES026) was already done; both halves of FBP014 are now resolved.
FBP014 | RESOLVED | Follow-up (SES034, FBT024): Multi-select gained a hover flyout — Select all / Nodes only / Paths only / Sketches only — Falcon (2026-09-05): "i want that when i hover over to multiselect i want it to have a secondary popup selection such as all (select all), paths only, nodes only, sketches only, (soon possible others)." Required extending the multi-select group itself from nodes-only to a mixed nodes+edges+sketches bag (`selection.ts`). Still RESOLVED overall — this is a refinement of the already-built tool, not new scope.
FBP015 | DEFERRED | Convert a node to another node kind in place — Falcon (2026-09-05, planning ahead): "tommorow will be covering features as follows 1. convert a node to another node." Not started; needs a design pass on what survives the conversion (existing wiring/anchors, config that doesn't map to the new kind, badge/counter state).
FBP016 | DEFERRED | Ribbon MODIFY group: Move/Flip(horizontal/vertical)/Rotate for paths and sketches only (not nodes), plus removing the now-redundant Pan/hand tool from HOME's Modify section — Falcon (2026-09-05): "additional modify features such as move,flip(horisontally,vertically),rotate(path and sketches only). but remove the redundant pan/hand on modify section on home tab." Not started.
FBP015 | DEFERRED | Falcon (2026-09-06), asked before conversion could be scoped: "WE NEED TO DISCUSS AN INDEPTH UNIQUE PROPERTIES SPECIFICATION FOR EACH NODE FIRST BEFORE WE DECIDE WHAT NODES IS CONVERTIBLE OR NOT... EACH NODE HAS EACH UNIQUENESS AND WE NEED TO DECIDE THE PORTS LOGIC HERE CAUSE SOME OF NODES HAS LIMITED PORT INPUTS AND OUTPUTS LOGIC." Still not started — waiting on that design discussion, not just an implementation session.
FBP016 | RESOLVED | Built (SES041, FBT031), after Falcon answered 3 clarifying questions: Move/Rotate reshape a selected path/sketch's interior only (endpoints pinned — "Reshape the curve, endpoints stay pinned"), Rotate free-drags with a magnetic 15° snap ("rotatable but snappable also when needed maintaining the shape"), Flip H/V mirrors the interior across the shape's own center axis. Falcon also asked, same answer, for a Group/Ungroup modify feature not in the original request: "i would like a group also as one of modify feature like use multiselect then those will get group into one group as a local group and in contrast to that... the ungroup/explode reverting the group into as before regardless of other modifications" — built as `skin/GroupRegistry.ts`, a persisted named set of member ids (no position/skin/logic meaning of its own); clicking any member re-selects the whole group, Ungroup dissolves just the grouping structure. Pan/hand tool removed from HOME's Modify section as asked (empty-canvas drag still pans for free).
FBP017 | DEFERRED | 3-point-arc sketch style — a third `SketchStyle` beyond Single path/Polypath (SES037), explicitly deferred at build time as future scope, not spec'd further yet.
FBP018 | DEFERRED | Camera positioning / scene recording power feature — Falcon: "even in the final phases of the builds i want the ultmate powerfeature to record scenes like a camera positioning?" A keyframeable virtual camera recorded out as video/GIF, explicitly a final-phase/late-build idea. Not started, no design conversation yet.
FBP019 | DEFERRED | Gate closed-state literal reverse-travel/bounce-back visual — flagged as a later Skin/Floor polish item during the Gate/Sensor build (SES042); not required for correct behavior since the existing backpressure convention already parks a refused item in place.
FBP020 | DEFERRED | Sensor edge-triggered pulse/latch model, as an alternative to today's level-based re-evaluation (a connected Gate's `open` state just tracks whatever the Sensor last evaluated, one tick behind) — flagged as a natural follow-up during the Gate/Sensor build (SES042) if Falcon wants true edge-triggered Gate behavior later.
FBP009 | DONE (rev.) | Reopened 2026-09-10 — Falcon, after testing the new Weighted round-robin Distributor mode live (screenshots: 3 wires, 1 "Port 0 weight" field): "how can i set it in the properties which port?", then, once the deeper ask was surfaced: "the distributor should be clockwise and the ports are named according to compass like the N,NE,SE,S,SW,W,NW." Confirmed via AskUserQuestion — ports auto-derived from the physical octagon anchor, applied to every port-based node kind (Distributor/Sorter/Mixer/Buffer), not just Distributor. The "keep them separate" call this id originally made (row above, 2026-09-03) is superseded: `sourcePort`/`targetPort` now just ARE the anchor index a wire is drawn from/to, everywhere, kept in sync by every anchor-reassignment call site. Distributor's existing ascending-sort round-robin order already satisfies "clockwise" for free (anchor 0=E round to 7=NE is already that convention) — no cycling-logic change needed. See FLUXBOARD.md FBT036, SESSIONS.md SES046, FBB006/FBF006.
